# WhatsAppMan site deploy has MOVED to its own repo:
#   server7_development/whatsappman-site  (deploys client/+server/ at root)
#
# This product repo no longer deploys the marketing site. Pipelines here are
# intentionally disabled so the two repos don't both restart the same PM2 app
# (whatsappman-site, port 50121). Re-enable by removing the workflow rule below.

workflow:
  rules:
    - when: never
